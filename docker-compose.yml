services:
  pythonapp:
    build:
      context: ./python_app
      dockerfile: Dockerfile
    restart: always
    pull_policy: build
    networks:
      traefik_srdnlen_website:
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.basiclfi.entrypoints=https"
      - "traefik.http.routers.basiclfi.tls=true"
      - "traefik.http.routers.basiclfi.tls.certresolver=letsencrypt"
      - "traefik.http.routers.basiclfi.rule=Host(`testingwebsite.chall.srdnlen.it`)"
  ghost:
    image: ghost:5-alpine
    restart: always
    ports:
      - 2368:2368
    environment:
      # see https://ghost.org/docs/config/#configuration-options
      database__client: mysql
      database__connection__host: db
      database__connection__user: root
      database__connection__password: example
      database__connection__database: ghost
      # this url value is just an example, and is likely wrong for your environment!
      url: http://localhost:2368
      # contrary to the default mentioned in the linked documentation, this image defaults to NODE_ENV=production (so development mode needs to be explicitly specified if desired)
      NODE_ENV: development

    volumes:
      - ghost:/var/lib/ghost/content

  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: example
    volumes:
      - db:/var/lib/mysql

volumes:
  ghost:
  db:
